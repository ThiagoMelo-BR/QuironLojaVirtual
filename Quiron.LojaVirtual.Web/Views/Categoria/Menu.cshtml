@model System.Collections.Generic.IEnumerable<string>

@Html.ActionLink("Todos os produtos","ListaProdutos","Vitrine",null, new {@class = "btn btn-block btn-default btn-lg"})

@foreach(var link in Model)
{
    @Html.RouteLink(link.TrimEnd(),new
{
    controller = "Vitrine",
    action = "ListaProdutos",
    categoria = link.TrimEnd(),
    pagina = 1
}, new
{
    @class = "btn btn-block btn-default btn-lg" +
        (link.TrimEnd() == ViewBag.CategoriaSelecionada ? " btn-primary" : "")
});
}